# Sprint 7

## Описание

Автотесты API учебного сервиса Яндекс Самокат.

В проекте реализованы проверки API для курьеров и заказов.

## Технологии

- Python
- pytest
- requests
- Allure

## Структура проекта

- `api.py` — класс с методами для работы с API
- `data.py` — тестовые данные
- `helpers.py` — генерация тестовых данных
- `conftest.py` — фикстуры и предусловия
- `tests/` — тестовые модули

### Тестовые модули

- `tests/test_create_courier.py` — тесты создания курьера
- `tests/test_login_courier.py` — тесты авторизации курьера
- `tests/test_create_order.py` — тесты создания заказа
- `tests/test_get_orders.py` — тест получения списка заказов
- `tests/test_delete_courier.py` — тесты удаления курьера
- `tests/test_accept_order.py` — тесты принятия заказа
- `tests/test_get_order.py` — тесты получения заказа по номеру

## Тестируемые ручки

### Создание курьера

Проверяется:

- успешное создание курьера
- создание курьера с существующим логином
- создание курьера без логина
- создание курьера без пароля

### Авторизация курьера

Проверяется:

- успешная авторизация
- неправильный логин
- неправильный пароль
- отсутствие логина

### Создание заказа

Проверяется:

- создание заказа без цвета
- создание заказа с чёрным цветом
- создание заказа с серым цветом
- создание заказа с двумя цветами

Тесты создания заказа выполняются с использованием параметризации.

### Получение списка заказов

Проверяется успешное получение списка заказов.

### Удаление курьера

Проверяется:

- успешное удаление
- удаление без идентификатора
- удаление с несуществующим идентификатором

### Принятие заказа

Проверяется:

- успешное принятие заказа курьером
- принятие заказа без идентификатора курьера

### Получение заказа по номеру

Проверяется:

- получение заказа по номеру
- запрос без номера
- запрос с несуществующим номером

## Запуск тестов

Для запуска всех тестов:

```bash
pytest -v